name: F1 Fantasy Reminders
on:
  schedule:
    - cron: '0 9 * * *' # Cada día a las 9 AM
  workflow_dispatch:

jobs:
  check-and-notify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Check F1 Calendar
        run: |
          echo "Ejecutando comprobación del calendario F1..."
          echo "Aviso: Revisa tu alineación para el próximo GP."
